# ============================================================
# Agent OS V6.0 - Secret (placeholder values)
# In production, use sealed-secrets or external-secrets operator
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: agent-os-secrets
  namespace: agent-os
  labels:
    app.kubernetes.io/name: agent-os
type: Opaque
stringData:
  DB_PASSWORD: "change-me-in-production"
  REDIS_PASSWORD: ""
  JWT_SECRET: "change-me-in-production-use-256-bit-key"
  LLM_API_KEY: ""